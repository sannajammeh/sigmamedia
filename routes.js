const routes = {
	home: '/',
	services: '/services',
	about: '/about',
	contact: '/contact',
	// elements: '/elements',
	skills: '/skills',
};

export const routeArray = Object.entries(routes);

export default routes;
